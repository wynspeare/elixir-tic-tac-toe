# defmodule Cells do
#   # defguard end_of_row(count, board) when rem(count, :math.sqrt(List.length(board))) == 0
#   defguard end_of_row(count, board_length) when rem(count, :math.sqrt(board_length)) == 0

# def end_of_row(count, board) when rem(count, :math.sqrt(List.length(board))) == 0
# def end_of_row(count, board_length) when rem(count, :math.sqrt(board_length)) == 0

#   # defguard end_of_row(count, _board) when count == 9
# end
